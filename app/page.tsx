'use client';
import {useEffect,useMemo,useState} from 'react';
import dynamic from 'next/dynamic';
import {Orbit,Globe2,Database,BookOpen,ArrowUpRight,Download,Plus,Minus,LocateFixed,Layers3,Check,X,Upload,FileText,ArrowRight,Search,SlidersHorizontal,RotateCcw,Pause,Play,Satellite,ShieldCheck} from 'lucide-react';
import {Reading,missions,distance,densityColor} from '@/lib/mission';
import ContributorAccess from '@/components/contributor-access';
import type {ArchiveResult,Marker} from '@/lib/archive';
const emptyArchive:ArchiveResult={markers:[],rows:[],total:0,matched:0,min:0,max:0,orbits:[],missions:[],histogram:Array(24).fill(0),aggregated:false};
const Globe=dynamic(()=>import('@/components/globe'),{ssr:false,loading:()=> <div className="globe-loading">Initializing Earth observatory…</div>});
const number=(n:number)=>n.toLocaleString('en-US');
export default function Home(){const [tab,setTab]=useState('Explorer'),[active,setActive]=useState([...missions]),[points,setPoints]=useState(true),[orbits,setOrbits]=useState(true),[grid,setGrid]=useState(false),[rotate,setRotate]=useState(false),[zoom,setZoom]=useState(0),[mode,setMode]=useState('Global'),[orbit,setOrbit]=useState('All orbits'),[day,setDay]=useState('All dates'),[minDensity,setMinDensity]=useState(0),[selected,setSelected]=useState<Marker|null>(null),[modal,setModal]=useState(''),[toast,setToast]=useState(''),[focus,setFocus]=useState<{lat:number;lon:number;key:number}|null>(null),[query,setQuery]=useState(''),[page,setPage]=useState(0),[lat,setLat]=useState('38.72'),[lon,setLon]=useState('-9.14'),[endLat,setEndLat]=useState('-23.55'),[endLon,setEndLon]=useState('-46.63'),[radius,setRadius]=useState('2000'),[region,setRegion]=useState<any>(null),[error,setError]=useState(''),[view,setView]=useState<'auto'|'aggregate'>('auto'),[resolution,setResolution]=useState<5|10>(10),[result,setResult]=useState<ArchiveResult>(emptyArchive),[loading,setLoading]=useState(true),[loadError,setLoadError]=useState('');
const filterKey=JSON.stringify({missions:active,orbit,day,minDensity,region,query,page,view,cellSize:resolution});
const data=result.markers,rows=result.rows;
useEffect(()=>setPage(0),[active,orbit,day,minDensity,region,query]);
useEffect(()=>{const controller=new AbortController();setLoading(true);setLoadError('');setResult(emptyArchive);setSelected(null);const timer=setTimeout(()=>{fetch('/api/readings?filter='+encodeURIComponent(filterKey),{signal:controller.signal}).then(r=>{if(!r.ok)throw new Error('The archive could not be loaded. Please try again.');return r.json()}).then(value=>setResult(value as ArchiveResult)).catch(e=>{if(e.name!=='AbortError')setLoadError(e.message)}).finally(()=>{if(!controller.signal.aborted)setLoading(false)});},150);return()=>{clearTimeout(timer);controller.abort()}},[filterKey]);
useEffect(()=>{if(!toast)return;const t=setTimeout(()=>setToast(''),4500);return()=>clearTimeout(t)},[toast]);
useEffect(()=>{const mc=(document as any).modelContext;if(!mc?.registerTool)return;const controller=new AbortController();try{Promise.resolve(mc.registerTool({name:'filter_mission_readings',description:'Filter visible synthetic plasma readings by mission, updating the explorer and export selection.',inputSchema:{type:'object',properties:{missions:{type:'array',items:{type:'string',enum:missions}}},required:['missions'],additionalProperties:false},annotations:{readOnlyHint:false},execute:(input:any)=>{if(!Array.isArray(input.missions)||input.missions.some((m:unknown)=>!missions.includes(m as string)))throw new Error('Choose supported mission names.');setActive(input.missions);return {missions:input.missions,source:'synthetic demonstration'};}},{signal:controller.signal})).catch(()=>{});}catch{}return()=>controller.abort()},[]);
function download(format='csv'){const a=document.createElement('a');a.href='/api/readings?filter='+encodeURIComponent(filterKey)+'&format='+format;a.download='icarus-ionis-selection.'+format;a.click();setToast('Your selection is being downloaded from the archive.');setModal('');}
function applyRegion(){const a={lat:Number(lat),lon:Number(lon)},b={lat:Number(endLat),lon:Number(endLon)},r=Number(radius);if(!lat.trim()||!lon.trim()||!radius.trim()||![a.lat,a.lon,r,...(mode==='Path'?[b.lat,b.lon]:[])].every(Number.isFinite)||Math.abs(a.lat)>90||Math.abs(a.lon)>180||r<1||r>20000||(mode==='Path'&&(!endLat.trim()||!endLon.trim()||Math.abs(b.lat)>90||Math.abs(b.lon)>180))){setError('Use latitude −90 to 90, longitude −180 to 180, and a radius from 1 to 20,000 km.');return;}if(mode==='Path'&&(distance(a,b)<1||distance(a,b)>19900)){setError('Choose distinct endpoints less than 19,900 km apart.');return;}setError('');setRegion({type:mode,a,b,radius:r});setFocus({...a,key:Date.now()});}
function reset(){setActive([...missions]);setDay('All dates');setOrbit('All orbits');setMinDensity(0);setRegion(null);setMode('Global');setError('')}
return <main className="app-shell"><header className="topbar"><button className="brand" onClick={()=>setTab('Explorer')} aria-label="IONIS home"><Orbit size={32}/><span>IONIS<small>ICARUS · PORTO SPACE TEAM</small></span></button><nav aria-label="Main navigation">{['Explorer','Data catalog','The mission'].map(t=><button className={tab===t?'nav-item active':'nav-item'} key={t} onClick={()=>setTab(t)}>{t==='Explorer'?<Globe2 size={16}/>:t==='Data catalog'?<Database size={16}/>:<Satellite size={16}/>} {t}</button>)}</nav><div className="header-actions"><span className="open-label">FREE IONOSPHERIC DATA</span><a className="team-link" href="https://github.com/PortoSpaceTeamNI" target="_blank" rel="noreferrer">Porto Space Team <ArrowUpRight size={15}/></a></div></header>
<div className="workspace-heading"><div><div className="eyebrow">ICARUS PROJECT / OPEN IONOSPHERIC DATA</div><h1>{tab==='Explorer'?'A new perspective on our ionosphere.':tab==='Data catalog'?'Every reading. Open to discovery.':'Small satellites. A shared understanding.'}</h1><p>{tab==='Explorer'?'Free access to ionospheric plasma data. An ICARUS project resource by Porto Space Team.':tab==='Data catalog'?'Inspect, filter, and extract measurements from the observatory.':'An open window into the charged environment surrounding our planet.'}</p></div><button className="demo-badge" onClick={()=>setModal('demo')}><span/> DEMONSTRATION DATA <BookOpen size={13}/></button></div>
{tab!=='The mission'?<><div className="explorer-layout"><aside className="side-panel"><div className="panel-title"><span><SlidersHorizontal size={16}/> Explore data</span><button className="icon-button" aria-label="Reset all filters" onClick={reset}><RotateCcw size={14}/></button></div><div className="filter-section"><label className="section-label" htmlFor="date">OBSERVATION PERIOD</label><select id="date" value={day} onChange={e=>setDay(e.target.value)}><option value="All dates">14 – 16 September 2026</option>{['2026-09-14','2026-09-15','2026-09-16'].map(d=><option key={d}>{d}</option>)}</select><div className="small-note">All timestamps in UTC</div></div><div className="filter-section"><div className="section-label between">DATA SOURCES <span>{active.length} / 3</span></div>{missions.map((m,i)=><label className="source-row" key={m}><input type="checkbox" checked={active.includes(m)} onChange={()=>setActive(active.includes(m)?active.filter(x=>x!==m):[...active,m])}/><span className={`source-dot source-${i}`}/><span>{m}<small>{i===2?'Technology demonstrator':'CubeSat constellation'}</small></span></label>)}</div><div className="filter-section"><div className="section-label">SPATIAL SELECTION</div><div className="segmented">{['Global','Coordinates','Path'].map(m=><button key={m} className={mode===m?'selected':''} onClick={()=>{setMode(m);setRegion(null);setError('')}}>{m}</button>)}</div>{mode==='Global'?<p className="small-note spatial-note">Explore all available measurement locations around the Earth.</p>:<div className="coordinate-form"><div className="input-pair"><label>Latitude<input type="number" value={lat} onChange={e=>setLat(e.target.value)} min="-90" max="90" step="any"/></label><label>Longitude<input type="number" value={lon} onChange={e=>setLon(e.target.value)} min="-180" max="180" step="any"/></label></div>{mode==='Path'&&<><div className="small-note">Destination · great-circle path</div><div className="input-pair"><label>End latitude<input type="number" value={endLat} onChange={e=>setEndLat(e.target.value)} step="any"/></label><label>End longitude<input type="number" value={endLon} onChange={e=>setEndLon(e.target.value)} step="any"/></label></div></>}<label>{mode==='Path'?'Corridor half-width':'Search radius'} (km)<input type="number" value={radius} onChange={e=>setRadius(e.target.value)}/></label>{error&&<p role="alert" className="error-text">{error}</p>}<button className="button subdued full" onClick={applyRegion}>Apply selection <LocateFixed size={14}/></button>{region&&<div className="small-note cyan">Spatial filter active · {result.total} readings</div>}</div>}<label className="field-label" htmlFor="orbit">Orbit</label><select id="orbit" value={orbit} onChange={e=>setOrbit(e.target.value)}><option>All orbits</option>{Array.from({length:9},(_,i)=><option key={i}>ORB-{1420+i}</option>)}</select></div><div className="filter-section"><label className="section-label" htmlFor="map-detail">MAP DETAIL</label><select id="map-detail" value={view} onChange={e=>setView(e.target.value as 'auto'|'aggregate')}><option value="auto">Automatic detail</option><option value="aggregate">Density overview</option></select><label className="field-label" htmlFor="cell-size">Overview cell size</label><select id="cell-size" value={resolution} onChange={e=>setResolution(Number(e.target.value) as 5|10)}><option value="10">10° · global overview</option><option value="5">5° · regional overview</option></select><p className="small-note">Large selections use spatial averages. Narrow a region to inspect individual readings.</p></div><div className="filter-section"><div className="section-label between">MAP LAYERS <Layers3 size={14}/></div>{[['Measurement points',points,setPoints],['Orbit trajectories',orbits,setOrbits],['Coordinate grid',grid,setGrid]].map(([name,value,fn])=><label className="toggle-row" key={name as string}><span>{name as string}</span><input className="switch" type="checkbox" checked={value as boolean} onChange={e=>(fn as any)(e.target.checked)}/></label>)}</div><div className="sidebar-footer"><ShieldCheck size={18}/><span>Built for open discovery<small>Free access. Reproducible science.</small></span></div></aside>
<section className={tab==='Explorer'?'earth-panel':'catalog-panel'} aria-label={tab==='Explorer'?'Earth explorer':'Data catalog'}>{tab==='Explorer'?<><div className="map-topline"><span className="map-label"><span className="pulse-dot"/> EARTH / 3D VIEW</span><span className="map-tag">WGS 84 <span>·</span> {region?'REGIONAL':'GLOBAL'} VIEW</span></div><Globe region={region} data={data} visibleOrbits={result.orbits} points={points} orbits={orbits} grid={grid} rotate={rotate} zoom={zoom} focus={focus} onSelect={setSelected}/><div className="map-caption"><span className="eyebrow">THE IONOSPHERE, IN FOCUS</span><h2>A planet of<br/>possibilities.</h2><p>{loading?'Loading archive…':number(result.total)+' readings to explore.'}</p><div className="map-detail-note">{result.aggregated?'SPATIAL MEANS':'INDIVIDUAL READINGS'} · {number(data.length)} markers<br/>{result.aggregated?resolution+'° cells · select one to explore nearby':'Up to 2,000 individual markers'}</div></div><div className="map-controls"><button aria-label="Zoom in" onClick={()=>setZoom(z=>Math.min(5,z+1))}><Plus size={18}/></button><button aria-label="Zoom out" onClick={()=>setZoom(z=>Math.max(-5,z-1))}><Minus size={18}/></button><button aria-label="Reset globe orientation" onClick={()=>{setZoom(0);setFocus({lat:25,lon:-35,key:Date.now()})}}><LocateFixed size={18}/></button><button aria-label={rotate?'Pause rotation':'Rotate Earth'} onClick={()=>setRotate(!rotate)}>{rotate?<Pause size={17}/>:<Play size={17}/>}</button></div><div className="map-bottomline"><span>Drag to rotate <b>·</b> Scroll to zoom <b>·</b> Select a point</span><span className="north-mark">N ↑</span></div>{loadError&&<div className="globe-error" role="alert">{loadError}</div>}{selected&&<div className="point-card"><div className="between"><span className="section-label">{selected.count?'DENSITY CELL':'MEASUREMENT'} {selected.id}</span><button className="icon-button" aria-label="Close measurement" onClick={()=>setSelected(null)}><X size={15}/></button></div><strong>{(selected.density/1e6).toFixed(3)} <small>× 10⁶ m⁻³</small></strong><p>{selected.lat.toFixed(3)}° / {selected.lon.toFixed(3)}° · {selected.alt} km</p>{selected.count?<><p>{number(selected.count)} readings · mean density</p><p>Range: {number(selected.minimum!)}–{number(selected.maximum!)} m⁻³</p><button className="button subdued full" onClick={()=>{const a={lat:selected.lat,lon:selected.lon};const r=Math.ceil(selected.cellSize!*111*.72);setLat(String(a.lat));setLon(String(a.lon));setRadius(String(r));setMode('Coordinates');setRegion({type:'Coordinates',a,b:a,radius:r});setFocus({...a,key:Date.now()});setView('auto');setSelected(null)}}>Explore nearby readings <ArrowRight size={14}/></button></>:<p>{selected.mission} · {selected.orbit}</p>}<small>{selected.time?selected.time.replace('T',' ').slice(0,19)+' UTC':'Spatial average across selected dates and altitudes'}</small><span className="quality">{selected.quality} · synthetic</span></div>}</>:<><div className="catalog-heading"><div><div className="eyebrow">MISSION ARCHIVE</div><h2>Measurement catalog</h2></div><div className="search-field"><Search size={16}/><input aria-label="Search readings" placeholder="Search ID, mission, orbit…" value={query} onChange={e=>setQuery(e.target.value)}/></div></div>{loading&&<p className="archive-status" role="status">Loading readings…</p>}{loadError&&<p className="archive-status error-text" role="alert">{loadError}</p>}<div className="table-scroll"><table><thead><tr><th>READING / UTC</th><th>COORDINATES</th><th>DENSITY (m⁻³)</th><th>MISSION / ORBIT</th><th>QUALITY</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td><button className="table-link" onClick={()=>{setSelected(r);setFocus({lat:r.lat,lon:r.lon,key:Date.now()});setTab('Explorer')}}>{r.id}</button><small>{r.time.slice(0,16).replace('T',' ')}</small></td><td>{r.lat.toFixed(2)}°, {r.lon.toFixed(2)}°<small>{r.alt} km altitude</small></td><td><span style={{color:densityColor(r.density)}}>{number(r.density)}</span></td><td>{r.mission}<small>{r.orbit}</small></td><td><span className={r.quality==='Validated'?'quality':'quality amber'}>{r.quality}</span></td></tr>)}</tbody></table>{!loading&&!loadError&&result.matched===0&&<div className="empty-state"><Database/><h3>No readings match this selection</h3><p>Broaden your spatial filter or enable another source.</p><button className="button subdued" onClick={()=>{reset();setQuery('')}}>Reset filters</button></div>}</div><div className="pagination"><span>{number(result.matched)} readings · synthetic dataset</span><div><button disabled={loading||page===0} onClick={()=>setPage(p=>p-1)}>Previous</button><span>{page+1} / {Math.max(1,Math.ceil(result.matched/10))}</span><button disabled={loading||(page+1)*10>=result.matched} onClick={()=>setPage(p=>p+1)}>Next</button></div></div></>}</section>
<aside className="insights-panel"><div className="section-label between">SELECTION OVERVIEW <span className="live-label">DEMO</span></div><div className="reading-count">{loading?'…':number(result.total)}<span>measurements in selection</span></div><div className="mini-stats"><div><span>{result.orbits.length}</span><small>Orbits</small></div><div><span>{result.missions.length}</span><small>Satellites</small></div></div><div className="insight-block"><div className="section-label">ELECTRON DENSITY</div><div className="density-range"><strong>{result.total?(result.min/1e6).toFixed(2):'—'} <span>—</span> {result.total?(result.max/1e6).toFixed(2):'—'}</strong><small>× 10⁶ electrons / m³</small></div><div className="color-scale"/><div className="scale-labels"><span>Low</span><span>High</span></div><label className="range-label">Minimum density <span>{(minDensity/1e6).toFixed(1)} × 10⁶</span><input aria-label="Minimum electron density" type="range" min="0" max="1200000" step="100000" value={minDensity} onChange={e=>setMinDensity(Number(e.target.value))}/></label></div><div className="insight-block"><div className="section-label">ALTITUDE PROFILE</div><div className="altitude-chart"><div className="altitude-bars">{Array.from({length:24},(_,i)=>{const n=result.histogram[i];return <i key={i} style={{height:`${Math.max(2,Math.min(95,n/Math.max(1,result.total)*700))}%`}}/>})}</div><div className="scale-labels"><span>452 km</span><span>508 km</span></div></div><p className="small-note">Low Earth orbit · synthetic altitude</p></div><div className="extract-card"><Download size={20}/><h3>Your next discovery<br/>starts with the data.</h3><p>Export your selection for your own analysis.</p><button className="button light full" onClick={()=>setModal('export')} disabled={loading||!result.total}>Export dataset <ArrowUpRight size={15}/></button><span>CSV or JSON · Open access</span></div></aside></div><div className="bottom-strip"><div><span className="status-ring"/> <strong>Science without boundaries.</strong> <span>An open ionosphere, for everyone.</span></div><button onClick={()=>setTab('The mission')}>Discover the mission <ArrowRight size={15}/></button></div></>:<section className="mission-page"><div className="mission-intro"><div className="eyebrow">ICARUS PROJECT · PORTO SPACE TEAM</div><h2>Make the invisible.<br/><span>Understandable.</span></h2><p>IONIS is the free ionospheric data observatory for the ICARUS project by Porto Space Team. Our aim is to make plasma measurements accessible for research, education and shared discovery, with no account required to explore or download public data.</p><button className="button light" onClick={()=>setTab('Explorer')}>Explore the observatory <ArrowUpRight size={16}/></button></div><div className="mission-principles">{[[Globe2,'01 / OBSERVE','A global perspective','Explore the locations where measurements were collected. Every point retains its coordinates, altitude, timestamp, and mission context.'],[Database,'02 / DISCOVER','Data you can work with','Select a region, follow a great-circle corridor, or isolate an orbit. Extract readings in CSV and JSON for analysis.'],[ShieldCheck,'03 / CONTRIBUTE','A shared scientific resource','Public access is free. Contributions require researcher evaluation, two-factor sign-in and an independent review of each dataset.']].map(([Icon,k,title,desc]:any)=><article key={k}><Icon size={25}/><div className="eyebrow">{k}</div><h3>{title}</h3><p>{desc}</p></article>)}</div><div className="access-banner"><div><div className="eyebrow">FOR RESEARCH PARTNERS</div><h3>Contribute through a trusted review process.</h3><p>Researcher access is separate from the public archive and subject to Porto Space Team approval.</p></div><button className="button subdued" onClick={()=>setModal('contribute')}>Contributor eligibility <ShieldCheck size={16}/></button></div><div className="mission-notice"><Satellite size={26}/><div><h3>A working frontend for the next phase.</h3><p>This observatory currently uses 2,160 synthetic readings across three illustrative spacecraft. A production mission database, researcher verification, and reviewed uploads are the next integration stage. No real mission results or operational spacecraft are represented.</p></div></div></section>}
<footer><span className="footer-brand"><Orbit size={16}/> IONIS <span>ICARUS project · © 2026 Porto Space Team · BOBDINHO · EduardoSilva</span></span><div><button onClick={()=>setModal('contribute')}>Contributor access</button><button onClick={()=>setModal('guide')}>Explorer guide</button><button onClick={()=>setModal('data')}>Data & attribution</button><span>DEMONSTRATION ARCHIVE <span className="cyan">v0.2</span></span></div></footer>
{modal&&<div className="modal-backdrop" onClick={()=>setModal('')}><section className="modal" role="dialog" aria-modal="true" aria-label={modal==='contribute'?'Contributor eligibility':modal==='export'?'Export dataset':'Observatory information'} onClick={e=>e.stopPropagation()} onKeyDown={e=>{if(e.key==='Escape')setModal('');if(e.key==='Tab'){const f=Array.from(e.currentTarget.querySelectorAll<HTMLElement>('button:not(:disabled),input,textarea,select,a[href]'));if(e.shiftKey&&document.activeElement===f[0]){e.preventDefault();f[f.length-1]?.focus()}else if(!e.shiftKey&&document.activeElement===f[f.length-1]){e.preventDefault();f[0]?.focus()}}}}><button className="modal-close icon-button" autoFocus onClick={()=>setModal('')} aria-label="Close dialog"><X size={22}/></button>{modal==='export'?<><div className="modal-icon"><Download/></div><div className="eyebrow">TAKE YOUR DISCOVERY FURTHER</div><h2>Export your selection.</h2><p>{number(result.total)} synthetic readings from {result.missions.length} spacecraft. Includes coordinates, UTC time, altitude, electron density, orbit, and quality labels.</p><div className="export-options"><button onClick={()=>download('csv')}><FileText/><strong>CSV spreadsheet</strong><span>Analysis tools & spreadsheets</span><ArrowUpRight/></button><button onClick={()=>download('json')}><Database/><strong>JSON dataset</strong><span>Structured data & research code</span><ArrowUpRight/></button></div><p className="small-note">Exports are explicitly marked as synthetic demonstration data.</p></>:modal==='contribute'?<ContributorAccess/>:<><div className="modal-icon"><BookOpen/></div><h2>{modal==='guide'?'A field guide to discovery.':modal==='data'?'Transparent by design.':'Explore freely. Interpret carefully.'}</h2>{modal==='guide'?<div className="guide-list"><p><b>01 · Navigate Earth</b>Drag to rotate, scroll or use + / − to zoom. Click a colored marker to inspect a reading or spatial mean. Above 2,000 readings, automatic detail uses bounded density cells. Select a cell and explore nearby to narrow the selection.</p><p><b>02 · Refine your selection</b>Toggle spacecraft, choose a date or orbit, and set a density threshold. Coordinate searches use a geodesic radius; paths use a great-circle corridor.</p><p><b>03 · Take the data with you</b>Export all filtered records as CSV or JSON. Hiding a map layer changes visibility only; the selection remains exportable.</p></div>:<><p>All 2,160 measurements in this preview are generated demonstration records. Spacecraft names, orbit identifiers, density values, and quality flags are illustrative and are not validated mission observations.</p><p>Electron density is expressed in m⁻³. Altitudes are in kilometres and positions use geographic latitude and longitude. Trajectories illustrate sampling geometry and are not propagated from real orbital elements.</p>{modal==='data'&&<p>Earth surface imagery: <a href="https://threejs.org/examples/?q=earth#webgl_shaders_earth" target="_blank" rel="noreferrer">Three.js Earth example</a>. IONIS is an ICARUS project resource by Porto Space Team. Credits: BOBDINHO and EduardoSilva. Public exploration and downloads are free; future mission datasets will state their own licensing and provenance.</p>}</>}</>}</section></div>}{toast&&<div className="toast" role="status"><Check size={18}/>{toast}</div>}</main>}
